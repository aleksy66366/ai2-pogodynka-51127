city,country,date,celsius,fahrenheit
{% for measurement in measurements %}
{{ city }},{{ country }},{{ measurement.date.format('Y-m-d') }},{{ measurement.celsius }},{{ measurement.fahrenheit }}
{% endfor %}